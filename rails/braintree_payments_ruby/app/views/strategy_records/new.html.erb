<div align='left'>
  <h3>Create Strategy</h3><br/><br/>
</div>
<div class="hero-unit">
	<% if current_algofast_user.is_registered_with_braintree? %>
		<%= render 'strategy_record' %>
	<% end %>
</div>

<div align='center'>
	<% if current_algofast_user.is_registered_with_braintree? %>
		<%= link_to "Manage Subscriptions",	subscriptions_path,
				class: "btn btn-large btn-primary" %>
	<% else %>
		<%= link_to "Subscribe now!", info_subscription_path, class: "btn btn-primary" %>
	<% end %>
</div>