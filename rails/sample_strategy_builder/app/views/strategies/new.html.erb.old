<!--<h1>Create Sample Events Page</h1>
<div class="row">
	<div class="span3 offset3">
      <%= field_set_tag "Strategy Information" do %>
            <%= form_for @strategy, :url => strategies_path do |f| %>

            	<%= f.fields_for @strategy.strategy_record do |record| %>
            		<%= render 'strategy_record', :f => record %>
            	<% end %>

            	<%= f.submit "Submit Strategy", class: "btn btn-large btn-primary" %>
            <% end %>
      <% end %>
	</div>
</div>-->